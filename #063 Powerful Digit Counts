import math

count = 0
limit = 1

while (10/9)**(limit-1) <= 9 :
  limit += 1

for n in range(1, limit + 1) :
  for k in range(1, 10) :
    if (n-1) * math.log(10) <= n * math.log(k) :
      count += 1

print(count)
