numerator = [x for x in range(11, 100) if x % 10 != 0] # 2-digit nums, except for 10 multiples
nums = [x for x in range(1,10)]

fractions = list()

for i in numerator :
  denominator = set([int(x + y) for x in list(map(str, nums)) for y in str(i)] + [int(y + x) for x in list(map(str, nums)) for y in str(i)]) # nums bigger than numerator
  denominator = [x for x in denominator if x > i]
  for j in denominator :
    i_digits = [x for x in str(i)]
    j_digits = [x for x in str(j)]
    intersection = list((set(i_digits) & set(j_digits)))
    
    for x in intersection :
      i_delx = str(i).replace(x, '') or x
      j_delx = str(j).replace(x, '') or x

      if i * int(j_delx) == j * int(i_delx) :
        fractions.append((i,j))

print(fractions)
