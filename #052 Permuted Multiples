digits = 2

def num_digits(x) :
	return set(sorted([int(digit) for digit in str(x)]))

button = False

while True :
	print(digits)
	for x in range(10 ** (digits - 1), 10 ** (digits)) :
		if all(num_digits(x) == num_digits(a * x) for a in [2, 3, 4, 5, 6]) :
			print(x)
			button = True
			break
	if button == True :
		break
	digits += 1
