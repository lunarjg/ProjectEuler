from fractions import Fraction
import math

def find_index(limit) :
  fracs = set()
  for d in range(1, limit + 1) :
    for n in range(math.ceil(d/3), int(math.floor(d/2)) + 1) :
      fracs.add(Fraction(n,d))
  return len(fracs) - 2

print(find_index(12000))
