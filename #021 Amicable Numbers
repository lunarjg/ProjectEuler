import math

def d(n) :
  divisors = {1}
  for i in range(2, math.isqrt(n) + 1) :
    if n % i == 0 :
      divisors.update({i, n / i})
  return int(sum(divisors))

print(sum(n for n in range(2,10000) if d(d(n)) == n and d(n) != n))
