import math

n = 2520

def gcd(a,b) :
  facs = []
  for k in range(1, min(a,b) + 1) :
    if a % k == 0 and b % k == 0 :
      facs.append(k)
  facs.sort()
  return facs[-1]

for i in range(11,21):
  n *= int(i / gcd(n, i))

print(n)
